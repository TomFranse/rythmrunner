# Setup States and Transitions

This setup-specific deep-dive has moved to feature-local documentation:

- `src/features/setup/docs/setup-states-and-transitions.md`

The root `documentation/` folder is reserved for cross-feature and project-wide docs.
