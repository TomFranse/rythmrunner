---
description: Cross-feature duplication and consolidation audit
argument-hint: <optional scope — full audit, feature pair, layer, or pattern>
---

# /consolidate

Read `.cursor/skills/consolidate/SKILL.md` and execute the consolidate workflow it describes for:

$ARGUMENTS

That file is the single source of truth. Follow it verbatim.

`/consolidate` is a **discovery and planning** pass — it finds redundancy and proposes a prioritized consolidation map. It does not implement changes without user approval after the presentation phase.
